:machine:
  java:
    version: openjdk7
checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mv config/.app.yml config/app.yml
 dependencies:
  - mvn clean verify
branches:
  only:
    - develop
test:
   - bash <(curl -s https://codecov.io/bash)

