machine:
  java:
    version: openjdk7
checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mv config/.app.yml config/app.yml
branches:
  only:
    - develop
deploy:
   - bash <(curl -s https://codecov.io/bash)
test:
