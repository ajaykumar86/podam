machine:
  java:
    version: openjdk7
checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mv config/.app.yml config/app.yml
dependencies:
  override:
    - mvn clean verify
branches:
  only:
    - develop
notify:
  webhooks:
   -url: https://codecov.io/bash

